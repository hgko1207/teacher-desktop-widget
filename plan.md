# 교사용 윈도우 데스크탑 위젯 - 개발 플랜

> 프로젝트명: **Teacher's Desk**
> 시작일: 2026-03-19
> 레퍼런스: [@kimju.zip 릴스](https://www.instagram.com/reel/DVqGsA-CYSP/) + [Gemini 디자인](https://gemini.google.com/share/4dfc5de5b3f1)
> 기술 스택: **Electron + React + TypeScript + Tailwind CSS + lucide-react**

---

## 완료된 작업

### ✅ 리서치 & 기획 (2026-03-19)
- [x] 교사 업무 환경 조사, 레퍼런스 릴스 분석, 기술 스택 비교
- [x] 나이스 Open API 엔드포인트 조사, 경쟁 제품 분석
- [x] research.md 작성 완료

### ✅ Phase 1: 프로젝트 셋업 & 기본 위젯 (2026-03-19)
- [x] Electron + electron-vite + React + TypeScript + Tailwind CSS 셋업
- [x] 메인 프로세스 (프레임리스 윈도우, 시스템 트레이, IPC)
- [x] 기본 위젯 7종 (시계, 시간표, 현재수업, 할일, D-Day, 메모, 급식)
- [x] Zustand 스토어, 커스텀 훅, lucide-react 아이콘 적용

---

## 다음 작업 (구현 예정)

### ✅ Phase 2: Gemini 디자인 완전 통합 (완료: 2026-03-19)
- [x] 풀스크린 3단+사이드바 레이아웃
- [x] 테마 3종 (인디고/벚꽃/민트) + 위젯 설정 모달
- [x] 신규 위젯: DesktopOrganizer, QuotesOffWork, LunchDday, SmartTools, SettingsModal
- [x] 시간표/현재수업 비주얼 업그레이드 (블라인드, 진행률 바, 펄스 애니메이션)

---

## 현재 기능 상태 (2026-03-19 기준)

| 위젯 | ✅ 동작 | 🔧 더미/하드코딩 | ❌ 미구현 |
|------|---------|----------------|----------|
| **시계** | 시간, 날짜, 요일 | 날씨 "맑음 12°C" 고정 | 날씨 API |
| **현재 수업** | 교시 감지, 진행률 바 | — | — |
| **시간표** | 추가/삭제, 저장, 블라인드 | — | 나이스 API 자동 로드 |
| **할 일** | 추가/완료/삭제, 저장 | — | — |
| **급식+D-Day** | D-Day 계산 | 급식 메뉴 고정, D-Day 날짜 고정 | 급식 API, D-Day 편집 |
| **퇴근+명언** | 퇴근 시간 계산, 랜덤 명언 | 퇴근 시간 16:30 고정 | 퇴근 시간 설정 |
| **스마트 도구** | NEIS/클래스팅 링크 | — | 타이머, 랜덤뽑기, 미제출자, 내선번호 |
| **설정 모달** | 테마 전환, 위젯 토글 | — | 학교 설정, 교시 시간 편집 |
| **바탕화면 정리** | UI 표시만 | — | (시각적 가이드 역할이라 OK) |

---

## Phase 3: 기능 구현 (다음 작업)

### ✅ Step 1: 스마트 도구 실제 구현 (완료: 2026-03-19)
- [x] **팝업 타이머**: 원형 SVG 진행률, 5/10/15/20/30분 프리셋, 커스텀 시간, 시작/정지/리셋, Web Audio 알림음, 자동닫기
- [x] **랜덤 학생 뽑기**: 학생 수 설정(저장), 번호 순환 애니메이션, "뽑힌 학생 제외" 토글, 초기화
- [x] **미제출자 체크**: 번호 격자 클릭 토글, 항목별 제목 입력, 미제출 카운트, 전체선택/해제, 항목별 저장
- [x] **내선번호 목록**: 학교 기본 번호 프리셋, 추가/편집/삭제, 검색, 로컬 저장
- [x] SmartToolsWidget에서 각 버튼 클릭 시 모달 오픈 연동
- [x] "빠른 5분 타이머" 버튼 → 타이머 모달 5분 프리셋으로 직접 오픈

---

### ✅ Step 2: D-Day 및 급식 기능 완성 (완료: 2026-03-19)
- [x] **DdayEditModal**: D-Day 추가/삭제, 핀(메인 지정), 색상 코딩 (7일↓빨강, 30일↓주황), 로컬 저장
- [x] **MealEditModal**: 급식 메뉴 수동 입력 (줄바꿈 구분), 칼로리 입력, 날짜별 저장
- [x] **LunchDdayWidget 연동**: 스토어에서 로드, 클릭 시 모달 오픈, 빈 상태 안내

---

### ✅ Step 3: 설정 모달 확장 (완료: 2026-03-19)
- [x] **탭 UI**: 테마&위젯 | 학교정보 | 시간설정 | 즐겨찾기 (4탭)
- [x] **학교 정보**: 학교명, 학교급(초/중/고), 학년/반, 지역(17개 시도), 학생수, NEIS API키
- [x] **시간 설정**: 7교시 시작/종료 시간 편집, 퇴근 시간 설정, 기본값 초기화
- [x] **즐겨찾기**: 런처 추가/편집/삭제/순서변경, 10색 컬러 선택
- [x] **연동**: 퇴근 위젯 offWorkTime 설정값 사용, 스마트도구 런처 설정값 사용

---

### ✅ Step 4: API 연동 (완료: 2026-03-19)
- [x] **날씨 API**: wttr.in 연동, 17개 지역→영문 도시 매핑, 영문→한글+이모지 날씨명, 30분 캐시
- [x] **급식 API**: 나이스 Open API 연동, 17개 교육청 코드 매핑, 메뉴 파싱 (알레르기 제거), 자동/수동 뱃지
- [x] **Electron IPC**: main process에서 net.fetch → preload API → renderer 연동
- [x] **ClockWidget**: 실시간 날씨 (기온, 날씨, 습도)
- [x] **LunchDdayWidget**: 학교코드 설정 시 자동 fetch, 새로고침 버튼, 로딩 스피너

#### 향후 추가 가능
- 나이스 시간표 API (`/hub/misTimetable`) - 학교코드+학년/반 → 자동 시간표 로드
- 에어코리아 미세먼지 API - 현재는 날씨만 표시

---

## Phase 4: 바탕화면 파티션 실제 기능 구현

### 개요
현재 좌측 파티션은 빈 점선 영역. 이를 실제 **파일/폴더를 드래그&드롭으로 배치**하고
**더블클릭으로 열 수 있는** 파일 정리 공간으로 만든다.

### Step 1: 파일/폴더 드래그 & 드롭 수신 🔴 최우선

#### 4-1. 파티션 영역에 파일 드롭 기능
- **구현**: 윈도우 탐색기에서 파일/폴더를 파티션 영역으로 드래그&드롭
- **기술**: HTML5 `dragover` + `drop` 이벤트 → `e.dataTransfer.files`로 경로 추출
- **Electron**: `webkitGetAsEntry()` 또는 `file.path` 속성으로 전체 경로 획득
- **저장**: 경로(path) + 이름(name) + 타입(file/folder) + 아이콘 → 로컬 저장소
- **카테고리**: 어떤 파티션에 드롭했는지에 따라 카테고리 자동 분류

#### 4-2. 파티션 카테고리 구성 (설정 가능)
- **기본 4개**: 운영계획 / 진행중 업무 / 나중에 볼 파일 / 기타
- **설정 모달에서 편집**: 이름 변경, 추가/삭제, 순서 변경
- **아이콘**: 카테고리별 색상 + Folder 아이콘

### Step 2: 배치된 파일/폴더 표시 + 조작 🔴 최우선

#### 4-3. 파일/폴더 아이콘 표시
- **구현**: 각 파티션 안에 배치된 파일/폴더를 아이콘+이름으로 표시
- **디자인**: 윈도우 바탕화면 아이콘과 유사 (작은 아이콘 + 이름 텍스트)
- **아이콘 타입**: 폴더(📁), 문서(.hwp/.docx → 📄), 엑셀(.xlsx → 📊), PDF(📕), 이미지(🖼️), 기타(📎)
- **그리드 배치**: 파티션 내에서 자동 그리드 배치 (윈도우 바탕화면처럼)

#### 4-4. 더블클릭으로 파일/폴더 열기
- **구현**: Electron `shell.openPath(path)` 호출
- **IPC**: renderer → preload → main process → `shell.openPath()`
- **폴더**: 탐색기로 열림
- **파일**: 연결된 기본 프로그램으로 열림 (.hwp → 한글, .xlsx → 엑셀 등)

#### 4-5. 우클릭 컨텍스트 메뉴
- **파일/폴더에서 우클릭**: "열기 / 폴더 위치 열기 / 파티션에서 제거"
- **빈 영역에서 우클릭**: "폴더 추가 / 파일 추가 (파일 선택 대화상자)"

### Step 3: 데이터 모델 + 저장 🟡 높음

#### 4-6. 타입 정의
```typescript
interface PartitionCategory {
  id: string
  name: string          // "운영계획"
  iconColor: string     // "#3b82f6"
  order: number
}

interface PartitionItem {
  id: string
  categoryId: string    // 어떤 파티션에 속하는지
  name: string          // 파일/폴더 이름
  path: string          // 전체 경로 "D:\교무\운영계획.hwp"
  type: 'file' | 'folder'
  extension: string     // "hwp", "xlsx", "" (폴더)
  addedAt: string       // ISO 날짜
}
```

#### 4-7. 저장소
- **Store key**: `partitionCategories` (카테고리 목록)
- **Store key**: `partitionItems` (배치된 파일/폴더 목록)
- **Zustand 스토어**: `partitionStore.ts` (CRUD + 저장/로드)

### Step 4: 바탕화면 고정 모드 🟢 보통 (선택)

#### 4-8. 윈도우를 바탕화면 레벨로 내리기
- **구현**: Electron BrowserWindow를 바탕화면 아래, 아이콘 위에 배치
- **기술**: Windows API `SetWindowPos` + `HWND_BOTTOM` 또는 `shell:desktop` 기법
- **참고**: Rainmeter가 사용하는 방식
- **리스크**: Windows 업데이트에 따라 불안정할 수 있음
- **대안**: "항상 뒤에(Always on Bottom)" 모드로 타협 가능

### 필요한 IPC 추가
```
'open-path'     → shell.openPath(path)
'open-folder'   → shell.showItemInFolder(path)
'select-file'   → dialog.showOpenDialog (파일 선택)
'select-folder' → dialog.showOpenDialog (폴더 선택)
```

---

## 일정 요약 (업데이트)

---

## 일정 요약

| Phase/Step | 내용 | 상태 |
|------------|------|------|
| Phase 1~2 | 프로젝트 셋업 + 디자인 | ✅ 완료 |
| Phase 3 Step 1 | 스마트 도구 4종 | ✅ 완료 |
| Phase 3 Step 2 | D-Day + 급식 | ✅ 완료 |
| Phase 3 Step 3 | 설정 모달 4탭 | ✅ 완료 |
| Phase 3 Step 4 | 날씨/급식 API 연동 | ✅ 완료 |
| Phase 4 | 파일 드래그&드롭 + 아이콘 + 열기 + 우클릭 + 저장 | ✅ 완료 |
| Phase 5 | 레퍼런스 기반 전면 개선 (파티션+레이아웃+문서번호) | ✅ 완료 |
| Phase 6 | 레이아웃 최종 정리 | ✅ 완료 |
| Phase 7 | 제미니 8단 그리드 레이아웃 | ✅ 완료 |
| Phase 8 | 마감 디테일 (간격/폰트/사이드바) | ✅ 완료 |
| Phase 9 | UI/UX 폴리싱 | ✅ 완료 |
| Phase 10 | 시간표 입력 모드 + 급식 수정 | ✅ 완료 |
| Phase 11 | 시간표 셀 정사각형 + 버튼 구성 | ✅ 완료 |
| Phase 12-1 | 미세먼지 실시간 (에어코리아 API) | ✅ 완료 |
| Phase 12-2 | 학사일정 위젯 (NEIS API) | ✅ 완료 |
| Phase 12-3 | 메모 업그레이드 (핸드폰 스타일) | ✅ 완료 |
| Phase 12-4 | 알림 시스템 (Electron Notification) | ✅ 완료 |
| Phase 12-5 | 바탕화면 고정 모드 (koffi + Windows API) | ✅ 완료 |
| 배포 | GitHub Releases v0.1.0 + 자동 업데이트 | ✅ 완료 |
| Phase 12-6 | 나이스 시간표 API (컴시간 보완) | 🔵 미구현 (보류) |

---

## Phase 5: 레퍼런스 분석 기반 전면 개선

> 사용자 피드백 + kimju.zip 영상 재분석 + 실사용자 요구사항 통합

### 5-0. 레퍼런스 영상 재분석 결과

**kimju.zip 바탕화면 구조 (2모니터)**:
```
[메인 모니터]                          [보조 모니터 (세로)]
┌──────────┬───────────────────┐     ┌──────────────────┐
│ 바탕화면   │ 현재시각 15:19     │     │ 앱 바로가기 아이콘  │
│ 빠른폴더  │ 2026.3.9 월요일    │     │ 지금 이 시간       │
│          │                   │     │ 7교시 공강 ☕      │
│ 2026행복  │ 주간 시간표        │     │ 15:00~15:45      │
│ (실제폴더) │ (색상 블록들)      │     │                  │
│          │                   │     │ 급식 정보 ◀오늘▶   │
│ 진행중업무 │                   │     │ • 기장밥          │
│ (새폴더)  │                   │     │ • 돈까스          │
│          │                   │     │ 856.3 Kcal       │
│ 나중에볼  │                   │     │                  │
│ (온등부)  │                   │     │ 학사 일정          │
│          │                   │     │ 1주/1달 (3월2주)   │
│ 기타     │                   │     │ 8 학교폭력예방주간  │
│ (행구리)  │                   │     └──────────────────┘
└──────────┴───────────────────┘
```

**핵심 관찰**:
1. 폴더 영역: 실제 Windows 폴더 아이콘이 **자연스럽게 배치** (엄격한 그리드X)
2. 각 영역에 **"실제 아이콘들을 이 칸에 모아두세요"** 안내 문구
3. 상단에 **"이 공간 위에 실제 윈도우 바탕화면 아이콘을 올려두고 사용하세요"** 가이드
4. 보조 모니터: 급식에 **◀ 오늘 ▶** 날짜 이동 버튼
5. 보조 모니터: **학사 일정** 위젯 (주간/월간 토글, 학교 행사 목록)
6. 할 일에 **체크하면 검은 원(●)** 표시

---

### Step 1: 파티션(좌측) 개선 🔴 최우선

#### 5-1. 폴더 추가 기능 보완
- **[+] 버튼 → 파일 또는 폴더 선택** (현재 파일만 가능 → 폴더도 가능하도록)
- 드롭다운: "파일 추가" / "폴더 추가" 선택지
- **우선순위**: 🔴

#### 5-2. 안내 문구 추가
- 파티션 최상단: **"바탕화면 빠른 폴더"** 타이틀
- 그 아래: **"이 공간에 실제 윈도우 바탕화면 아이콘을 올려두고 사용하세요"** 안내
- 각 카테고리 빈 영역: **"실제 아이콘들을 이 칸에 모아두세요"** 힌트 텍스트
- **우선순위**: 🔴

#### 5-3. 파일 배치 스타일 변경
- 현재: 엄격한 그리드 → 변경: **자연스러운 아이콘 배치** (레퍼런스 스타일)
- 아이콘 크기 키움 (현재 너무 작음)
- 파일명 아래에 표시, 긴 이름은 2줄까지 허용
- Windows 바탕화면 아이콘과 비슷한 느낌
- **우선순위**: 🟡

#### 5-4. 카테고리 기본값 변경
- 기본 4개: **운영계획 / 진행중 업무 / 나중에 볼 파일 / 기타**
- 설정 모달에서 카테고리 이름/순서/추가/삭제 편집 가능 (설정 5번째 탭)
- **우선순위**: 🟡

---

### Step 2: 레이아웃 재배치 🔴 최우선

#### 5-5. 즐겨찾기 런처 ↔ 급식+D-Day 위치 교체
- **현재**: 급식+D-Day(col-2) | 스마트도구+런처(col-2)
- **변경**: 런처를 우측 상단에 배치 (바로 누를 수 있도록)
- 급식+D-Day는 스마트도구 쪽으로 이동
- **배치안**:
  ```
  상단: 시계(7) | 현재수업(2) | 퇴근+명언(3)
  하단: 시간표(5) | 할일(3) | 급식+D-Day+런처+스마트도구(4)
  ```
  또는 우측을 세로 스택으로:
  ```
  우측 열 (col-4):
  ┌─ 즐겨찾기 런처 (컴팩트, 가로 아이콘 행) ─┐
  ├─ 급식 카드 ──────────────────────────┤
  ├─ D-Day 카드 (컴팩트) ────────────────┤
  ├─ 스마트 도구 ────────────────────────┤
  └────────────────────────────────────┘
  ```
- **우선순위**: 🔴

#### 5-6. 급식 위젯 크기 최적화
- 현재: 너무 세로로 길어서 빈 공간 많음
- 변경: 메뉴 항목이 적을 때 자동으로 높이 축소
- 칼로리를 메뉴 아래 한 줄로 표시
- 급식 날짜 이동 버튼 추가 (◀ 오늘 ▶) - 어제/내일 급식도 확인 가능
- **우선순위**: 🟡

---

### Step 3: 신규 기능 추가 🟡 높음

#### 5-7. 문서 관련번호 도구 (스마트 도구에 추가)
- **배경**: 교사가 공문/자료 처리 시 문서 관련번호를 조회해야 함
- **현재**: 메모장이나 엑셀에 따로 적어둠 → 찾기 불편
- **구현**: 스마트 도구에 "문서번호" 버튼 추가 → 모달 팝업
- **기능**:
  - 문서번호 목록 (제목 + 번호 + 메모)
  - 추가/편집/삭제
  - 검색/필터
  - 번호 클릭 → 클립보드 복사
  - 카테고리 분류 (공문, 내부결재, 기안문 등)
- **데이터 타입**:
  ```typescript
  interface DocumentNumber {
    id: string
    title: string      // "2026학년도 교육과정 운영계획"
    number: string     // "교무-2026-0042"
    category: string   // "공문" | "내부결재" | "기안문"
    memo: string       // 추가 메모
    addedAt: string
  }
  ```
- **저장**: electron store key 'documentNumbers'
- **UI**: 내선번호 모달과 유사한 패턴
- **우선순위**: 🟡 높음

#### 5-8. 학사 일정 위젯 (레퍼런스 보조 모니터 기능)
- **판단**: 현재 우리 앱은 단일 모니터 → 공간 부족으로 **Phase 6으로 미룸**
- **대안**: D-Day 위젯에 "이번 주 학사일정" 미니 목록 추가 가능
- **우선순위**: 🟢 보통 (나중에)

#### 5-9. 할 일 체크 디자인 개선
- 레퍼런스: 체크하면 **검은 원(●)** + 취소선
- 현재: 테마색 체크박스
- 선택: 레퍼런스 스타일이 더 직관적이면 변경
- **우선순위**: 🔵 낮음

---

### 구현 순서 요약

| 순서 | 작업 | 내용 |
|------|------|------|
| **1** | 5-1, 5-2 | 폴더 추가 + 안내 문구 (파티션) |
| **2** | 5-5, 5-6 | 레이아웃 재배치 + 급식 크기 |
| **3** | 5-7 | 문서 관련번호 도구 |
| **4** | 5-3, 5-4 | 파일 배치 스타일 + 카테고리 설정 |

---

---

## Phase 6: 레이아웃 최종 정리 (사용자 피드백 반영)

### 전체 레이아웃 구조
```
┌──────────┬────────────────────────────────────────────────────────────┐
│          │ [시간+날짜] [날씨(지역+아이콘+기온)] │ [지금이시간+퇴근타이머(넓게)] │ [런처: 투명, 우상단]
│ 바탕화면  ├──────────┬───────────────┬─────────────────────────────────┤
│ 빠른폴더  │          │               │                                 │
│ (320px)  │ 시간표    │  나의 할 일    │  명언 ("오늘 하루도 수고했어요")   │
│          │ (현재의   │  (flex-1)     │  급식 정보 (세로로 크게, 5-6메뉴) │
│          │  75% 크기)│───────────────│  스마트 도구                     │
│          │          │  D-Day        │                                 │
│          │          │───────────────│                                 │
│          │          │  메모          │                                 │
└──────────┴──────────┴───────────────┴─────────────────────────────────┘
```

### 변경 사항

#### 6-1. 시간표 크기 축소
- 현재 `flex-1` → **고정 너비로 줄이기** (현재의 약 75%)
- 교시 라벨, 요일 헤더, 셀 크기 그대로 유지하되 전체 폭 줄임

#### 6-2. 즐겨찾기 런처 → 우측 상단 오버레이
- 현재: 별도 열 차지 → **우측 상단에 투명하게 떠있는 형태**
- 반투명 배경, 작은 아이콘 가로 배치
- "위젯 편집 및 테마" 버튼도 여기 옆에 배치, 글자/버튼 크게

#### 6-3. 상단 행 재배치
- **시간+날짜**: 컴팩트 (시계+날짜 뱃지만)
- **날씨 정보**: 별도 카드로 분리 (지역명 + 날씨 아이콘 + 기온 + 최저/최고 + 미세먼지)
  - 4번째 이미지 스타일: "오늘 날씨 / 구미 / ☁ 8° / 구름조금 최저-4°/최고8° / 미세39 보통"
- **지금 이 시간 + 퇴근 타이머**: 조금 더 넓게 배치

#### 6-4. 중앙 열 (할일 쪽) 재구성
- **나의 할 일** (flex-1, 메인)
- **D-Day** (컴팩트 카드)
- **메모** (신규 복원, 텍스트 입력 영역)

#### 6-5. 우측 열 재구성
- **명언** ("오늘 하루도 수고했어요" 등 힐링 문구)
- **급식 정보** (세로로 크게, 메뉴 5-6개 잘 보이도록)
- **스마트 도구** (타이머/랜덤뽑기/미제출자/내선번호/문서번호)
- 이 세 영역 **지금보다 아주 조금만 넓힘**

#### 6-6. 메모 위젯 복원
- 이전에 삭제된 MemoWidget 복원
- 간단한 텍스트 입력, 자동 저장
- 노란색 그라데이션 배경 (이전 디자인)

---

## Phase 7: 제미니 레이아웃 최종 적용 (8단 그리드)

> 제미니 디자인의 8단 그리드를 적용하여 상단/하단 완벽 정렬

### 레이아웃 구조
```
[좌측 사이드바 320px] | [메인 영역 flex-1]
                      |
                      | 헤더: [N NEIS] [W 업무포털] [C 클래스팅]          [⚙ 설정]
                      |
                      | 상단 (100px, 8단 그리드):
                      | [시계 2칸] [날씨 2칸] [현재수업 2칸] [퇴근+명언 2칸]
                      |
                      | 하단 (flex-1, 8단 그리드):
                      | [시간표 3칸] [할일+D-Day+메모 3칸] [스마트도구+급식 2칸]
```

### 핵심 변경사항

#### 7-1. 8단 그리드 시스템 (grid-cols-8)
- 상단 4개 카드: 각 2칸씩 = 8칸 (완벽 정렬)
- 하단 3개 열: 3칸 + 3칸 + 2칸 = 8칸 (상단과 완벽 일치)
- **상단과 하단 경계선이 일직선으로 정렬됨**

#### 7-2. 상단 정보 행 (100px, 컴팩트)
- **시계 (2칸)**: 큰 시간 + 초 + 날짜 뱃지 (인디고)
- **날씨 (2칸)**: "오늘 날씨 서울" + 온도 + 아이콘 + 최저/최고
- **현재수업 (2칸)**: "지금 이 시간" + 수업명 + 진행률 바 + 남은시간
- **퇴근+명언 (2칸)**: "행복한 퇴근까지 6시간 11분" + 구분선 + 명언

#### 7-3. 하단 3열 (flex-1)
- **시간표 (3칸)**: 월~금 그리드, 기존 기능 유지
- **할일+D-Day+메모 (3칸)**: 세로 스택
  - 할 일 (flex-3): 메인 영역
  - D-Day (shrink-0): 컴팩트 카드
  - 메모 (flex-1.5): 노란색 메모장
- **스마트도구+급식 (2칸)**: 세로 스택
  - 스마트 도구 (flex-1.2): 2x3 그리드
  - 급식 (flex-1): 센터 정렬, 큰 글자, 알레르기 정보

#### 7-4. 좌측 사이드바 유지 (320px)
- 현재 바탕화면 빠른 폴더 **그대로 유지** (파일 드래그&드롭 기능 포함)
- 제미니 스타일은 적용하지 않음

#### 7-5. 런처 = 상단 헤더 (현재 유지)
- TitleBar에 통합된 런처 아이콘 유지

#### 7-6. 카드 스타일 통일 (제미니 스타일)
- `background: 'rgba(255,255,255,0.8)'` (좀 더 불투명)
- `border: '1px solid rgba(226,232,240,0.6)'` (slate-200/60)
- `borderRadius: '24px'`
- `boxShadow: '0 2px 10px -4px rgba(0,0,0,0.02)'` (매우 연한 그림자)
- 배경: `#F8F9FB` (제미니 배경색)

#### 7-7. 급식 카드 디자인 (제미니 스타일)
- 메뉴 항목 **센터 정렬, 큰 글자 (14px bold)**
- 메인 메뉴: extrabold, 서브: semibold
- 하단: 알레르기 정보 (하트 아이콘 + 작은 텍스트)
- 배경: `white → orange-50/30` 그라데이션
- "중식" 뱃지 (오렌지)

### 구현 순서
1. App.tsx 레이아웃을 8단 그리드로 전면 교체
2. 상단 4개 위젯 높이 100px로 컴팩트하게
3. 하단 3:3:2 비율 적용
4. 카드 스타일 통일 (제미니 스타일)
5. 급식 카드 디자인 변경
6. 퇴근+명언 한 카드에 통합

---

## ✅ Phase 8: 마감 디테일 (간격/폰트/사이드바) (완료)

#### 8-1. 카드 간격 통일
- 현재: 상단 행과 하단 행 사이 간격이 다름
- 수정: 모든 gap을 **동일한 12px (gap-3)**으로 통일
- App.tsx의 `gap-4`를 `gap-3`으로, 내부도 `gap-3`으로 맞춤

#### 8-2. "Teacher's Desk" 좌측 사이드바 통합
- TitleBar에서 "Teacher's Desk" 제거 (이미 제거됨)
- **DesktopOrganizer 상단에 배치**: "● Teacher's Desk" 타이틀 + 폴더 영역
- 사이드바가 앱의 브랜드 홈 역할

#### 8-3. 설정 버튼 크기 키우기
- 현재: 작은 "설정" 텍스트
- 변경: 패딩 늘리고, 폰트 키우고, hover 효과 강화

#### 8-4. 글자 크기 설정 제거 + 고정 크기
- zoom 방식은 하단 잘림 유발 → **제거**
- 적당한 고정 크기(14px)로 통일
- 설정 모달에서 글자 크기 옵션 제거

#### 8-5. 상단/하단 간격 확보
- grid 내부 gap이 상단 행과 하단 행 사이에 적용 안 됨
- **해결**: 상단/하단을 grid가 아닌 flex-col로 분리하여 명시적 gap

#### 8-6. 날씨 미세먼지 복원 ✅

---

## ✅ Phase 9: UI/UX 폴리싱 (시니어 디자이너 관점) (완료)

> 10년차 디자이너/개발자 관점에서 전체 앱을 검토.
> 교사가 매일 8시간+ 보는 화면이므로 **가독성, 시각적 피로도, 정보 계층** 중심.

### 9-1. 카드 border-radius 조정
- **현재**: 24px (너무 둥글어서 장난감 같은 느낌)
- **변경**: **16px** (모던하면서 프로페셔널)
- 모든 위젯 카드에 일괄 적용

### 9-2. 상단 카드 내부 여백 (breathing room)
- **현재**: p-3 또는 p-4 (빡빡함)
- **변경**: **p-4** 통일 + 내부 요소 간 **gap 확보**
- 100px 높이에서 위아래 여유를 줘서 답답하지 않게

### 9-3. 날씨 위젯 정보 계층 재설계
- **타이틀**: "오늘 날씨" **왼쪽 정렬** (아이콘 오른쪽 아닌 타이틀 옆)
- **지역명**: 타이틀과 명확히 구분 (색상 분리, 볼드)
- **온도**: 가장 크고 눈에 띄게
- **미세먼지**: 뱃지 간 간격 넓히고, 수치+상태 읽기 좋게
- 전체적으로 **줄 간격(line-height)** 넉넉하게

### 9-4. 전체 UI 일관성 검토
- **타이틀 스타일 통일**: 모든 위젯 타이틀 동일 크기/색상/무게
  - 아이콘 + 타이틀: 14px bold, color #334155
  - 서브텍스트: 11px medium, color #94a3b8
- **정보 계층**:
  - 1차 정보 (시간, 수업명, D-Day 숫자): 크고 bold
  - 2차 정보 (날짜, 설명): 중간
  - 3차 정보 (힌트, 부가): 작고 연한 색
- **클릭 영역**: 최소 32px 높이 (모바일 가이드라인 차용)
- **호버 효과**: 클릭 가능한 요소에만 hover 표시

### 9-5. 시간표 가독성 개선
- 교시 라벨: 글자 크기 키우기 (현재 너무 작음)
- 요일 헤더: 더 또렷하게
- 빈 셀: 너무 진하지 않게 (현재 적당하면 유지)

### 9-6. 할 일 입력 UX 개선
- 입력창 placeholder 글자 크기 통일
- 빈 상태 문구 위치 조정

---

## ✅ Phase 10: 시간표 입력 모드 + 급식 수정 (완료)

### 10-1. 시간표 입력 모드 (교사 유형별)

현재 시간표는 "반 번호(1-4, 2-2)" 하나만 입력할 수 있음.
교사 유형에 따라 입력 방식이 달라야 함:

#### A. 중등 교과 교사 (현재 방식 유지 + 개선)
- 여러 반에 들어가므로 **반 번호** 입력 (1-4, 2-2 등)
- 공강 = 빈 셀 (검은 블록)
- 추가: **과목도 함께 입력** 가능 (선택) → "수학 1-2" 형태
- 컴시간 자동 불러오기 지원

#### B. 초등 담임 교사
- 한 반만 담당하므로 **과목명** 입력 (국어, 수학, 영어 등)
- 반 번호 불필요
- 컴시간으로 자동 불러오기 가능

#### C. 특수반(도움반) 교사
- 한 반만 있으므로 초등처럼 **과목명** 입력
- 컴시간 연동은 보통 불가 → **수기 입력만** 지원

#### 구현 방안
- 설정에 **"시간표 입력 모드"** 옵션 추가:
  - `'class'` = 반 번호 입력 (중등 교과용, 기본값)
  - `'subject'` = 과목 입력 (초등 담임 / 특수반용)
- 시간표 셀 편집 시 모드에 따라 입력 필드 변경
- `'class'` 모드: "반" placeholder → "1-4"
- `'subject'` 모드: "과목" placeholder → "국어"

### 10-2. 학교 검색 드롭다운 수정
- **문제**: 동명 학교(충남 정산중 vs 전북 정산중)가 나이스에서 병합 시 덮어쓰기됨
- **해결**: 나이스 결과를 이름이 아닌 **schoolCode 기준**으로 매칭
- 드롭다운에 **지역 + 주소** 표시하여 구분

### 10-3. 급식 로드 수정
- **문제**: 학교 선택 후에도 급식이 안 나옴
- **원인 확인**: schoolCode/eduCode가 제대로 저장되는지
- naeilsMap에서 같은 이름의 학교가 덮어쓰기됨 → schoolCode 기반으로 변경
- 급식 자동 로드: 앱 시작 시 + 학교 변경 시

### 10-4. 시간표 상세 동작

#### 중등 교과 교사 (반 번호 모드)
- 셀에 **"반 번호"** 입력 (예: 1-4, 2-2)
- 의미: "이 시간에 N반에 수업 들어감"
- 오늘 요일의 **공강 셀 = 검은 블록**으로 강조 (레퍼런스 이미지)
- 지나간 교시 = 회색 블라인드 (이미 구현됨)
- 컴시간 연동: 로직 유지 (나중에 활용 가능)

#### 초등/특수반 교사 (과목 모드)
- 셀에 **"과목명"** 입력 (예: 국어, 수학)
- 한 반의 전체 수업이므로 과목이 들어감
- 초등학교는 컴시간 연동 가능 (검토 결과: comcigan에서 초등도 지원)
- 특수반은 수기 입력만

### 10-5. 급식 안 불러와지는 문제 디버깅
- **확인 완료**: 나이스 API는 정상 작동 (테스트: 3/20 급식 확인)
- **오늘(토요일)은 급식 없음**: `INFO-200` 정상 응답
- **앱에서 안 되는 이유 추정**:
  1. 학교 검색 후 `schoolCode`/`eduCode`가 저장 안 됨 (병합 로직 문제)
  2. `net.fetch`가 Electron에서 정상 작동하지만 타이밍 문제
- **해결**: 디버그 로그 추가하여 확인, 월요일에 실제 테스트

### 구현 순서
1. ✅ 학교 검색 수정 (동명 학교 구분)
2. ✅ 시간표 입력 모드 추가
3. 급식 디버그 로그 추가 (터미널에서 원인 확인)
4. ✅ 오늘 요일 전체 컬럼 = 검은 블록 강조

---

## ✅ Phase 11: 시간표 셀 정사각형 + 입력 모드 선택 (완료)

### 11-1. 시간표 셀 정사각형
- **현재**: 가로 넓은 직사각형 (height: 48px, width: auto)
- **변경**: **정사각형** (aspect-ratio: 1 또는 동일 width/height)
- table 셀에 `aspect-ratio: 1` 적용

### 11-2. 시간표 버튼 구성

#### 헤더 버튼 배치 (왼→오)
```
주간 시간표          [초기화] [자동불러오기*] [✏ 수기 수정]
```
- **수기 수정**: `Pencil` 아이콘 + "수기 수정" 텍스트. 토글 (클릭→편집모드→"✓ 완료")
- **자동불러오기**: `Download` 아이콘. auto 모드 + 학교 설정 시에만 표시
- **초기화**: `RotateCcw` 아이콘. 시간표 항목이 1개 이상일 때만 표시.
  - 디자인: 연한 빨간 배경 (#fef2f2), 작은 크기
  - 클릭 시 confirm 대화상자: "시간표를 모두 초기화할까요?"
  - 모드 전환 시 데이터 자동 삭제 안 함 (안전)

---

## Phase 12: 추가 기능 (미세먼지/학사일정/알림/메모/바탕화면)

### ✅ 12-1. 미세먼지 실시간 연동 (완료)

**현재**: 에어코리아 API 연동 완료, 30분 캐시, API 키 없으면 폴백

**방법 분석**:
- 에어코리아 API: **인증키 필요** (data.go.kr 가입, 무료, 일 500건)
- wttr.in: 미세먼지 데이터 없음
- **대안**: 나이스처럼 KEY 없이 되는 API 없음 → **에어코리아 API 키 입력 방식**

**구현**:
- 설정에 `airApiKey` 필드 추가 (에어코리아 API 키)
- API: `http://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty`
- 파라미터: `sidoName=충남` (설정의 region 매핑)
- 응답에서 PM10(미세), PM2.5(초미세) 수치 + 등급 추출
- 30분 캐시
- API 키 없으면 현재처럼 "보통" 표시 (폴백)
- **우선순위**: 🟡 높음

### ✅ 12-2. 학사 일정 위젯 (완료)

**현재**: SchoolScheduleWidget 구현 완료, NEIS API 연동, 24시간 캐시

**API 확인**: 나이스 SchoolSchedule **KEY 없이 작동 확인!**
- 정산중학교 3월: 3·1절, 입학식, 개학식, 기초학력진단평가 등

**구현**:
- 나이스 API: `/hub/SchoolSchedule` (KEY 불필요)
- 이번 달 일정 자동 로드 (schoolCode + eduCode 필요)
- **배치**: 우측 열 명언 카드 자리를 학사 일정으로 교체 (또는 명언 아래)
- UI: 날짜 + 행사명 리스트, 오늘 기준 다가오는 일정 강조
- **우선순위**: 🟡 높음

### ✅ 12-3. 메모 위젯 업그레이드 (핸드폰 메모 스타일) (완료)

**현재**: 여러 메모 관리, 리스트/편집 모드, 생성/수정/삭제 완료

**구현**:
- **리스트 뷰**: 메모 목록 (제목 + 미리보기 + 날짜)
- **새 메모 작성**: [+] 버튼 → 편집 모달/인라인
- **메모 편집**: 메모 클릭 → 내용 편집
- **메모 삭제**: 스와이프 또는 삭제 버튼
- **데이터 타입**:
  ```typescript
  interface MemoItem {
    id: string
    title: string      // 첫 줄 또는 자동 생성
    content: string    // 전체 내용
    updatedAt: string  // 마지막 수정 시간
    createdAt: string
  }
  ```
- **저장**: electron store key 'memos' (배열)
- **UI**: 리스트 모드 ↔ 편집 모드 토글
  - 리스트: 메모 카드들 세로 스크롤
  - 편집: 제목 + 본문 textarea + 저장/취소
- **우선순위**: 🟡 높음

### ✅ 12-4. 알림 시스템 (완료)

**구현 완료**:
- Electron `Notification` API 사용 (Windows 알림 센터 연동)
- **알림 종류**:
  1. 수업 시작 N분 전 ("다음 교시 1-4반 수업입니다")
  2. 쉬는 시간 종료 전 ("곧 N교시가 시작됩니다")
  3. 퇴근 시간 알림 ("퇴근 시간입니다!")
- **설정**: 알림 ON/OFF, 수업 전 알림 시간 (5분/10분)
- **기술**: `setInterval`로 매 분마다 체크, 조건 맞으면 Notification 발송
- **우선순위**: 🟢 보통

### ✅ 12-5. 바탕화면 고정 모드 (완료: 2026-03-26)
- [x] `koffi` 패키지로 Windows API `SetWindowPos(HWND_BOTTOM)` 직접 호출
- [x] `windowPin.ts` 모듈: pinToDesktop / unpinFromDesktop
- [x] 1초 interval로 유지 + 포커스 없을 때만 re-pin (위젯 조작 가능)
- [x] TitleBar에 📌 고정 버튼 추가 (활성: 인디고, 비활성: 회색)
- [x] 앱 시작 시 이전 설정 자동 복원, 종료 시 Z-order 정리

### ✅ 배포 세팅 (완료: 2026-03-26)
- [x] `electron-builder.yml` 설정 (NSIS 인스톨러 + 포터블)
- [x] `electron-updater` 자동 업데이트 (GitHub Releases 기반)
- [x] `gh release create v0.1.0` 첫 배포 완료
- [x] 배포 파일: TeacherDesk-0.1.0-Setup.exe / TeacherDesk-0.1.0-Portable.exe
- [x] 릴리즈 URL: https://github.com/hgko1207/teacher-desktop-widget/releases/tag/v0.1.0

### 12-6. 나이스 시간표 API (컴시간 보완)

**현재**: 컴시간 직접 구현으로 시간표 자동 불러오기
**추가**: 나이스 시간표 API도 지원 (KEY 필요 여부 테스트 필요)
- API: `/hub/misTimetable` (중등), `/hub/elsTimetable` (초등)
- 컴시간이 안 될 때 폴백으로 사용
- **우선순위**: 🔵 낮음

### 구현 순서 (우선순위)

| 순서 | 항목 | 상태 |
|------|------|------|
| **1** | 12-2. 학사 일정 | ✅ 완료 |
| **2** | 12-3. 메모 업그레이드 | ✅ 완료 |
| **3** | 12-1. 미세먼지 | ✅ 완료 |
| **4** | 12-4. 알림 | ✅ 완료 |
| **5** | 12-5. 바탕화면 고정 | ✅ 완료 |
| **6** | 배포 세팅 | ✅ 완료 (v0.1.0) |
| 7 | 12-6. 나이스 시간표 | 미구현 (컴시간으로 충분) |

### ✅ 11-3. 입력 모드 3가지 (완료: 2026-03-21)
- [x] **반 번호 입력** (중등 교과): "1-4", "2-2" 등 학년-반 입력
- [x] **과목 자동 불러오기** (컴시간 연동): 버튼 클릭 → 자동 채움
- [x] **과목 수동 입력** (초등 담임/특수반): "국어", "수학" 직접 입력
- [x] `timetableMode: 'class' | 'subject'` settingsStore에 저장, TimetableWidget CellEditor 연동

---

*최종 업데이트: 2026-03-29*

---

## Phase 13: 바탕화면 고정 모드 (Desktop Pin Mode)

### 목표
위젯 창이 모든 앱 뒤에 고정되어 바탕화면처럼 동작.
사용자가 다른 창을 클릭해도 위젯이 올라오지 않음.

### 기술 선택: koffi + SetWindowPos (방법 A)

**근거**:
- `koffi`: node-ffi-napi의 현대적 대체제, Electron 28 + Node 18 호환, 네이티브 빌드 불필요
- `SetWindowPos(hwnd, HWND_BOTTOM)`: Z-order를 맨 아래로 → 모든 창 뒤로
- WorkerW 임베드(방법 B)는 자식창 제약 + 해상도 변경 시 깨짐 → 제외

---

### Step 1: koffi 설치 및 HWND 획득

#### 13-1. koffi 패키지 추가
```bash
npm install koffi
```

#### 13-2. Electron 창 HWND 얻기
```typescript
// src/main/index.ts
import { getNativeWindowHandle } from 'electron'
const hwnd = mainWindow.getNativeWindowHandle() // Buffer (8bytes on x64)
const hwndBigInt = hwnd.readBigUInt64LE(0)
```

---

### Step 2: Windows API 래퍼 모듈

#### 13-3. src/main/windowPin.ts 작성
```typescript
import koffi from 'koffi'

const user32 = koffi.load('user32.dll')

const SetWindowPos = user32.func(
  'SetWindowPos',
  'bool',
  ['uint64', 'uint64', 'int', 'int', 'int', 'int', 'uint']
)

const SetWindowLongPtr = user32.func(
  'SetWindowLongPtrW',
  'intptr',
  ['uint64', 'int', 'intptr']
)

const HWND_BOTTOM = BigInt(1)
const HWND_NOTOPMOST = BigInt(-2)
const SWP_NOMOVE = 0x0002
const SWP_NOSIZE = 0x0001
const SWP_NOACTIVATE = 0x0010
const SWP_FLAGS = SWP_NOMOVE | SWP_NOSIZE | SWP_NOACTIVATE

export function pinToDesktop(hwnd: bigint): void {
  SetWindowPos(hwnd, HWND_BOTTOM, 0, 0, 0, 0, SWP_FLAGS)
}

export function unpinFromDesktop(hwnd: bigint): void {
  SetWindowPos(hwnd, HWND_NOTOPMOST, 0, 0, 0, 0, SWP_FLAGS)
}
```

---

### Step 3: Electron 메인 프로세스 연동

#### 13-4. 고정 모드 토글 로직 (src/main/index.ts)
```typescript
let pinInterval: NodeJS.Timeout | null = null
let isPinned = false

function enableDesktopPin(win: BrowserWindow): void {
  const hwnd = win.getNativeWindowHandle().readBigUInt64LE(0)
  isPinned = true
  // 즉시 아래로
  pinToDesktop(hwnd)
  // focus 이벤트마다 다시 아래로 (다른 창 클릭 후 돌아올 때 방지)
  win.on('focus', () => { if (isPinned) pinToDesktop(hwnd) })
  // 1초마다 Z-order 유지 (안전망)
  pinInterval = setInterval(() => { if (isPinned) pinToDesktop(hwnd) }, 1000)
}

function disableDesktopPin(win: BrowserWindow): void {
  isPinned = false
  if (pinInterval) { clearInterval(pinInterval); pinInterval = null }
  const hwnd = win.getNativeWindowHandle().readBigUInt64LE(0)
  unpinFromDesktop(hwnd)
}
```

#### 13-5. IPC 핸들러 추가
```typescript
ipcMain.handle('toggle-desktop-pin', async (_, enable: boolean) => {
  if (enable) enableDesktopPin(mainWindow)
  else disableDesktopPin(mainWindow)
  return true
})

ipcMain.handle('get-desktop-pin', async () => isPinned)
```

---

### Step 4: 렌더러 UI 연동

#### 13-6. preload/index.ts에 API 추가
```typescript
toggleDesktopPin: (enable: boolean) => ipcRenderer.invoke('toggle-desktop-pin', enable),
getDesktopPin: () => ipcRenderer.invoke('get-desktop-pin'),
```

#### 13-7. TitleBar에 핀 버튼 추가
- 현재 TitleBar에 설정 버튼(⚙️), 최소화, 닫기 있음
- 📌 핀 아이콘 버튼 추가 (Lucide `Pin` / `PinOff`)
- 토글 시 배경색 변경 (활성: 인디고, 비활성: 기본)
- 상태는 `window.api.getDesktopPin()`으로 초기 로드

#### 13-8. 설정 저장
- electron-store에 `desktopPin: boolean` 저장
- 앱 시작 시 `desktopPin === true`면 자동으로 고정 모드 활성화

---

### Step 5: 엣지 케이스 처리

#### 13-9. 주���사항
- **UAC 다이얼로그**: 관리자 권한 창은 항상 위에 표시됨 (정상)
- **전체화면 앱**: 전체화면 게임/영상 재생 시 위젯이 보이지 않음 (정상)
- **다중 모니터**: 창이 있는 모니터에서만 동작 (정상)
- **앱 종료 시**: disableDesktopPin 호출하여 Z-order 정리

#### 13-10. 안전 해제
```typescript
app.on('before-quit', () => {
  if (isPinned) disableDesktopPin(mainWindow)
})
```

---

### 구현 순서

| 순서 | 항목 | 예상 시간 |
|------|------|-----------|
| 1 | koffi 설치 + windowPin.ts 작성 | 30분 |
| 2 | main/index.ts IPC 핸들러 연동 | 20분 |
| 3 | preload API 추가 + 타입 정의 | 10분 |
| 4 | TitleBar 핀 버튼 UI | 20분 |
| 5 | 시작 시 자동 고정 (설정 저장) | 10분 |
| 6 | 테스트 및 엣지 케이스 확인 | 20분 |

**총 예상**: 약 2시간

### 리스크 평가

| 리스크 | 가능성 | 대응 |
|--------|--------|------|
| koffi 빌드 오류 | 낮음 | koffi는 사전 빌드 바이너리 제공 |
| Windows 업데이트 후 동작 변경 | 낮음 | SetWindowPos는 수십 년간 안정적 API |
| 1초 interval 성능 영향 | 매우 낮음 | SetWindowPos는 O(1) 네이티브 호출 |
| 포커스 이벤트 루프 | 낮음 | isPinned 플래그로 방어 |

### ✅ 구현 완료 (2026-03-25)
- [x] koffi 설치 + windowPin.ts (pinToDesktop / unpinFromDesktop)
- [x] main/index.ts: enableDesktopPin / disableDesktopPin + IPC 핸들러
- [x] preload: toggleDesktopPin / getDesktopPin API
- [x] TitleBar: 📌 핀 버튼 (활성: 인디고, 비활성: 회색)
- [x] 앱 시작 시 desktopPin 설정 자동 복원
- [x] before-quit 시 Z-order 정리

---

## Phase 14: 배포 세팅 (GitHub Releases + 자동 업데이트)

### 목표
비개발자(교사)도 쉽게 설치할 수 있는 Windows 인스톨러 + 포터블 배포.
GitHub Releases를 통한 버전 관리 + 앱 내 자동 업데이트.

### ✅ 구현 완료 (2026-03-25)

#### 14-1. electron-builder.yml 정비
- [x] appId: `com.teacher-widget.app`
- [x] productName: `Teacher's Desk`
- [x] Win 타겟: **NSIS 인스톨러** + **포터블** (둘 다 x64)
- [x] NSIS: 한국어 인터페이스, 설치 경로 변경 가능, 바탕화면 바로가기
- [x] 포터블: `TeacherDesk-버전-Portable.exe`
- [x] koffi 네이티브 모듈 asarUnpack 추가
- [x] GitHub publisher 설정 (`owner: hgko1207, repo: teacher-desktop-widget`)

#### 14-2. electron-updater 연동
- [x] electron-updater 설치 (6.8.3)
- [x] 앱 시작 5초 후 자동 업데이트 확인
- [x] 업데이트 발견 시 백그라운드 자동 다운로드
- [x] 다운로드 완료 시 Windows 알림 표시
- [x] 앱 종료 시 자동 설치 (`autoInstallOnAppQuit: true`)
- [x] 개발 환경에서는 비활성 (`app.isPackaged` 체크)

#### 14-3. package.json 스크립트 추가
- [x] `release:win`: `npm run build && electron-builder --win --publish always`

### 배포 방법 (릴리즈 절차)

```bash
# 1. 버전 업 (package.json의 version 수정)
# 예: "version": "0.1.0" → "0.2.0"

# 2. 커밋 & 태그
git add -A
git commit -m "chore: bump version to 0.2.0"
git tag v0.2.0
git push && git push --tags

# 3. GitHub Token 설정 (최초 1회)
$env:GH_TOKEN = "ghp_xxxxxxxxxxxxxxxx"

# 4. 빌드 + GitHub Releases 자동 업로드
npm run release:win
```

**결과**: `dist/` 폴더에 아래 2개 파일 생성 + GitHub Releases에 자동 업로드
- `TeacherDesk-0.2.0-Setup.exe` (인스톨러)
- `TeacherDesk-0.2.0-Portable.exe` (포터블)

### GitHub Token 발급 방법
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. 권한: `repo` 전체 체크
4. 생성된 토큰을 `GH_TOKEN` 환경변수로 설정
